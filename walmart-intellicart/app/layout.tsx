import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { AuthProvider } from "@/hooks/use-auth"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Walmart IntelliCart - Smart Grocery Shopping Assistant",
  description: "AI-powered grocery shopping with smart recommendations, household sync, and in-store navigation",
  keywords: "grocery, shopping, AI, smart cart, walmart, household, navigation",
  authors: [{ name: "Walmart IntelliCart Team" }],
  viewport: "width=device-width, initial-scale=1",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  )
}
